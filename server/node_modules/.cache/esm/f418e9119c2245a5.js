let User;_9c2‍.x([["register",()=>register],["login",()=>login]]);_9c2‍.w("../modules/user",[["default",["User"],function(v){User=v}]]);

       const register = async (req, res) => {
    _9c2‍.g.console.log(req);
    //haa  '
    const {name, email, password} = req.body;
    if(!name) return res.status(400).send("Name is required");
    if(!password || password.length < 6)
        return res
        .status(400)
        .send("Password is required and should be min 6 characters long"); 
    let userExist = await User.findOne({email}).exec();
    if(userExist) return res.status(400).send('Email is taken')

    const user = new User(req.body)
    try {
        await user.save();
        _9c2‍.g.console.log("USER CREAT)ED", user);
        return res.json({ ok: true});
    } catch (err) {
        _9c2‍.g.console.log('CREATE USER FAILED', err)
        return res.status(400).send("Error. Try again. ")
    }

};

       const login = async (req, res) => {
    _9c2‍.g.console.log("request : ", req.body);
    _9c2‍.g.console.log("result : ", res);
};
